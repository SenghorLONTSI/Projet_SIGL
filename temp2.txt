                    </p>
                  )}
                </div>

                {/* Infos TP */}
                <div className="rounded-md border border-slate-100 bg-slate-50/70 px-3 py-2">
                  <div className="flex items-center justify-between gap-2">
                    <p className="text-[11px] font-semibold text-slate-600 uppercase tracking-wide">
                      Tuteur pÃ©dagogique (TP)
                    </p>
                    <span
                      className={`px-2 py-[2px] rounded-full text-[10px] font-semibold ${
                        a.tp
                          ? "bg-emerald-100 text-emerald-700"
                          : "bg-amber-100 text-amber-700"
                      }`}
                    >
                      {a.tp ? "AssignÃ©" : "Non assignÃ©"}
                    </span>
                  </div>

                  {a.tp ? (
                    <div className="mt-1 space-y-1.5">
                      <p className="text-xs font-medium text-slate-800">
                        {a.tp.name} {a.tp.subName ?? ""}
                      </p>
                      {a.tp.user?.email && (
                        <p className="text-[11px] text-slate-600 truncate">
                          <span className="text-slate-500">Email : </span>
                          {a.tp.user.email}
                        </p>
                      )}
                    </div>
                  ) : (
                    <p className="mt-2 text-[11px] text-slate-500">
                      Aucun TP n&#39;est encore associÃ© Ã  cet apprenti.
                    </p>
                  )}
                </div>
              </CardContent>

              <div className="flex flex-row items-center justify-end px-4  pt-2">
                <Button
                  variant="outline"
                  size="sm"
                  onClick={() => router.push(`/journal/ma/${a.id}`)}
                >
                  AccÃ©der aux journaux
                </Button>
              </div>
            </Card>
          ))}
        </div>
      )}
    </div>
  );
}
